#define threshold 8 //Threshold for tracer activation. Lower number is more sensisitive
#define pulselength 20 //Tracer LED pulse length in ms
#define startupflash 100 //Startup & calibration flash length in ms
#define maxpulses 20 //Maximum number of pulses before the tracer recalibrates
#define startup_flashes 2 //Number of flashes when the unit powers on
#define calibration_flashes 3 //Number of flashes when the unit has calibrated
#define diagmode false //Enable diagnostic mode to output sensor data to serial
